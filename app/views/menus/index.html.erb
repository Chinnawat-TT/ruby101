<div class="flex flex-col gap-4 p-4 bg-gray-100 rounded-lg shadow-md">

    <p class="text-lg font-semibold text-gray-800">Menu</p>


    <%= link_to 'Recommended', "menus/recommended", class: "text-blue-500 hover:text-blue-700 font-medium underline" %>


    <div class="flex flex-col gap-4">
        <% @menus.each do |menu| %>
        <%= render 'card', item: menu %>
        <% end %>
    </div>
</div>